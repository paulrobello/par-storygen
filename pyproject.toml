[project]
name = "par-storygen"
dynamic = [
    "version",
]
description = "AI-driven TUI choose-your-own-adventure"
readme = "README.md"
url = "https://github.com/paulrobello/par-storygen"
authors = [
    { name = "Paul Robello", email = "probello@gmail.com" },
]
maintainers = [
    { name = "Paul Robello", email = "probello@gmail.com" },
]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Environment :: Console",
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: End Users/Desktop",
    "Intended Audience :: Other Audience",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows :: Windows 10",
    "Operating System :: Microsoft :: Windows :: Windows 11",
    "Operating System :: POSIX :: Linux",
    "Topic :: Terminals",
    "Topic :: Games/Entertainment",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Typing :: Typed",
]
keywords = [
    "storygen",
    "choose-your-own-adventure",
    "interactive-fiction",
    "openai",
    "ollama",
    "openrouter",
    "gemini",
    "llm",
    "ai",
    "terminal",
    "tui",
    "textual",
]
requires-python = ">=3.13"
dependencies = [
    # ARC-014: bound high-churn deps to >=current,<MAJOR+1 so a major
    # release cannot land via a bare `uv lock` / `uv sync` without an
    # intentional bump here. uv.lock pins the exact resolved version.
    "textual>=8.2.8,<9",
    "pydantic>=2.13.4",
    "pydantic-ai>=2.11.0,<3",
    "openai>=2.45.0,<3",
    "rich>=15.0.0",
    "par-textual-image>=0.1.0",
    "typer>=0.27.0",
    "xdg-base-dirs>=6.0.2",
    "httpx>=0.28.1",
    "pillow>=12.3.0",
    "tenacity>=9.1.4",
    "python-dotenv>=1.2.2",
    "jinja2>=3.1.6",
    "google-genai>=2.12.1",
    "textual-pyfiglet>=1.0.0",
    "par-cli-tts>=0.5.1",
]
packages = [
    "src/storygen",
]

[project.license]
file = "LICENSE"

[project.urls]
Homepage = "https://github.com/paulrobello/par-storygen"
Documentation = "https://github.com/paulrobello/par-storygen/blob/main/docs/ARCHITECTURE.md"
Source = "https://github.com/paulrobello/par-storygen"
Issues = "https://github.com/paulrobello/par-storygen/issues"
Discussions = "https://github.com/paulrobello/par-storygen/discussions"

[project.scripts]
storygen = "storygen.main:app"
storygen-api = "storygen_api.main:cli"

[project.optional-dependencies]
api = [
    "fastapi>=0.139.2",
    "uvicorn[standard]>=0.51.0",
    "websockets>=16.1",
    "python-multipart>=0.0.32",
]

[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "pytest-mock>=3.15.1",
    "pytest-timeout>=2.4.0",
    "ruff>=0.15.22",
    "pyright>=1.1.411",
    "pre-commit>=4.6.0",
    "hatchling>=1.31.0",
]

[tool.hatch.version]
path = "src/storygen/__init__.py"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = [
    "src/storygen",
    "src/storygen_api",
]

[tool.hatch.build.targets.sdist]
include = [
    "src/storygen",
    "src/storygen_api",
    "web",
    "LICENSE",
    "README.md",
    "pyproject.toml",
]
exclude = [
    "*.pyc",
    "__pycache__",
    "*.so",
    "*.dylib",
]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "B",
    "UP",
    "SIM",
    "RUF",
]
ignore = [
    "E501",
]

# FastAPI's documented idiom is `Depends(...)` in default arguments (and the
# reusable `Annotated[..., Depends()]` aliases in security.py / rate_limit.py
# don't cover every router site). B008 would force a rewrite of ~22 router
# signatures for no functional gain; the per-file-ignore matches FastAPI's
# own recommendation in RUF013/B008 docs.
[tool.ruff.lint.per-file-ignores]
"src/storygen_api/**" = ["B008"]

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "strict"
include = [
    "src",
    "tests",
]
exclude = [
    "web",
]
reportMissingTypeStubs = false

[tool.pytest.ini_options]
testpaths = [
    "tests",
]
addopts = "-v --strict-markers"
asyncio_mode = "auto"
timeout = 10

[tool.uv.sources]
